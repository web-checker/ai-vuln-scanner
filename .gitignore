# ── Node / Vite (프론트엔드) ──
node_modules/
dist/
.vite/

# ── Python ──
__pycache__/
*.py[cod]
.venv/
venv/

# ── 진단 입력/출력 데이터(유저 업로드 CSV·생성 보고서) ──
*.csv
*.xlsx

# ── 영속 저장소(자산/진단 이력 — 런타임 생성, 커밋 안 함) ──
data/

# ── Claude Code 로컬 도구 ──
.claude/
CLAUDE.md

# ── OS / 임시 ──
.DS_Store
*.log
